#include "minishell.h"

t_node  *parser(t_token *tokens)
{
    t_iter_tkn tokens_itr;
    t_nodes     *nodes;

    tokens_itr = tokens;
    nodes = parse_cmdline(tokens);
    return (t_node *)tokens;
}
